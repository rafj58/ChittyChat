syntax = "proto3";

option go_package = "grpc/proto";

package proto;

message Message {
    string  clientName   = 1;
    string  msg          = 2;
    int64   time         = 3;
}

service proto {
    rpc AskForMessages(stream Message) returns (stream Message);
}
